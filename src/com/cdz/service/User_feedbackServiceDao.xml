<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--用户反馈建议[user_feedback]SQL语句 -->
<mapper namespace="User_feedback">
	<select id="listUser_feedbacksPage" parameterType="Dto" resultType="Dto">
		SELECT
		<include refid="dao.User_feedbackDao.column"/>
		FROM user_feedback
		<where>
 		</where>
	</select>
</mapper>